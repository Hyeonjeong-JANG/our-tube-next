# Clerk Authentication (https://clerk.com)
# 1. Clerk Dashboard에서 새 애플리케이션 생성
# 2. API Keys 탭에서 아래 키들을 복사
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/
# 3. Webhooks 섹션에서 엔드포인트 추가 후 서명 키 복사
CLERK_SIGNING_SECRET=whsec_...

# Neon PostgreSQL Database (https://neon.tech)
# 1. Neon Console에서 새 프로젝트 생성
# 2. Connection Details > Pooled connection 문자열 복사
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Upstash Redis (https://upstash.com)
# 1. Upstash Console에서 새 Redis 데이터베이스 생성
# 2. REST API 탭에서 아래 값들 복사
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Upstash QStash/Workflow
# 1. Upstash Console > QStash > Workflow 탭
# 2. 토큰 및 서명 키 복사
QSTASH_TOKEN=...
# 3. 개발환경: ngrok URL, 프로덕션: 배포된 도메인
UPSTASH_WORKFLOW_URL=https://your-domain.com
QSTASH_CURRENT_SIGNING_KEY=sig_...
QSTASH_NEXT_SIGNING_KEY=sig_...

# Mux Video Processing (https://mux.com)
# 1. Mux Dashboard > Settings > Access Tokens
# 2. 새 토큰 생성 (권한: Video 읽기/쓰기)
MUX_TOKEN_ID=...
MUX_TOKEN_SECRET=...
# 3. Settings > Webhooks에서 엔드포인트 추가 후 비밀키 복사
MUX_WEBHOOK_SECRET=...

# UploadThing File Upload (https://uploadthing.com)
# 1. UploadThing Dashboard에서 새 앱 생성
# 2. API Keys 탭에서 토큰 복사
UPLOADTHING_TOKEN=...
